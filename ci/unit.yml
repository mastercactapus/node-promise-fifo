---
platform: linux
image_resource:
  type: docker-image
  source:
    repository: node
    tag: 4
inputs:
  - name: project-src
run:
  path: bash
  args:
    - -c
    - |
      set -e
      cd project-src
      npm install
      npm test

